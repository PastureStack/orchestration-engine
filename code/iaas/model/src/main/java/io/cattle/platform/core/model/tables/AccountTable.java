/*
 * This file is generated by jOOQ.
 */
package io.cattle.platform.core.model.tables;


import io.cattle.platform.core.model.CattleTable;
import io.cattle.platform.core.model.Indexes;
import io.cattle.platform.core.model.Keys;
import io.cattle.platform.core.model.tables.AccountLinkTable.AccountLinkPath;
import io.cattle.platform.core.model.tables.AgentTable.AgentPath;
import io.cattle.platform.core.model.tables.AuditLogTable.AuditLogPath;
import io.cattle.platform.core.model.tables.AuthTokenTable.AuthTokenPath;
import io.cattle.platform.core.model.tables.BackupTable.BackupPath;
import io.cattle.platform.core.model.tables.BackupTargetTable.BackupTargetPath;
import io.cattle.platform.core.model.tables.CertificateTable.CertificatePath;
import io.cattle.platform.core.model.tables.ConfigItemStatusTable.ConfigItemStatusPath;
import io.cattle.platform.core.model.tables.ContainerEventTable.ContainerEventPath;
import io.cattle.platform.core.model.tables.CredentialTable.CredentialPath;
import io.cattle.platform.core.model.tables.DeploymentUnitTable.DeploymentUnitPath;
import io.cattle.platform.core.model.tables.DynamicSchemaTable.DynamicSchemaPath;
import io.cattle.platform.core.model.tables.ExternalEventTable.ExternalEventPath;
import io.cattle.platform.core.model.tables.GenericObjectTable.GenericObjectPath;
import io.cattle.platform.core.model.tables.HealthcheckInstanceHostMapTable.HealthcheckInstanceHostMapPath;
import io.cattle.platform.core.model.tables.HealthcheckInstanceTable.HealthcheckInstancePath;
import io.cattle.platform.core.model.tables.HostLabelMapTable.HostLabelMapPath;
import io.cattle.platform.core.model.tables.HostTable.HostPath;
import io.cattle.platform.core.model.tables.HostTemplateTable.HostTemplatePath;
import io.cattle.platform.core.model.tables.ImageTable.ImagePath;
import io.cattle.platform.core.model.tables.InstanceLabelMapTable.InstanceLabelMapPath;
import io.cattle.platform.core.model.tables.InstanceLinkTable.InstanceLinkPath;
import io.cattle.platform.core.model.tables.InstanceTable.InstancePath;
import io.cattle.platform.core.model.tables.IpAddressTable.IpAddressPath;
import io.cattle.platform.core.model.tables.LabelTable.LabelPath;
import io.cattle.platform.core.model.tables.MountTable.MountPath;
import io.cattle.platform.core.model.tables.NetworkDriverTable.NetworkDriverPath;
import io.cattle.platform.core.model.tables.NetworkTable.NetworkPath;
import io.cattle.platform.core.model.tables.NicTable.NicPath;
import io.cattle.platform.core.model.tables.PhysicalHostTable.PhysicalHostPath;
import io.cattle.platform.core.model.tables.PortTable.PortPath;
import io.cattle.platform.core.model.tables.ProcessInstanceTable.ProcessInstancePath;
import io.cattle.platform.core.model.tables.ProjectMemberTable.ProjectMemberPath;
import io.cattle.platform.core.model.tables.ProjectTemplateTable.ProjectTemplatePath;
import io.cattle.platform.core.model.tables.RegionTable.RegionPath;
import io.cattle.platform.core.model.tables.ResourcePoolTable.ResourcePoolPath;
import io.cattle.platform.core.model.tables.ScheduledUpgradeTable.ScheduledUpgradePath;
import io.cattle.platform.core.model.tables.SecretTable.SecretPath;
import io.cattle.platform.core.model.tables.ServiceConsumeMapTable.ServiceConsumeMapPath;
import io.cattle.platform.core.model.tables.ServiceEventTable.ServiceEventPath;
import io.cattle.platform.core.model.tables.ServiceExposeMapTable.ServiceExposeMapPath;
import io.cattle.platform.core.model.tables.ServiceIndexTable.ServiceIndexPath;
import io.cattle.platform.core.model.tables.ServiceLogTable.ServiceLogPath;
import io.cattle.platform.core.model.tables.ServiceTable.ServicePath;
import io.cattle.platform.core.model.tables.SnapshotTable.SnapshotPath;
import io.cattle.platform.core.model.tables.StackTable.EnvironmentPath;
import io.cattle.platform.core.model.tables.StorageDriverTable.StorageDriverPath;
import io.cattle.platform.core.model.tables.StoragePoolTable.StoragePoolPath;
import io.cattle.platform.core.model.tables.SubnetTable.SubnetPath;
import io.cattle.platform.core.model.tables.UserPreferenceTable.UserPreferencePath;
import io.cattle.platform.core.model.tables.VolumeTable.VolumePath;
import io.cattle.platform.core.model.tables.VolumeTemplateTable.VolumeTemplatePath;
import io.cattle.platform.core.model.tables.ZoneTable.ZonePath;
import io.cattle.platform.core.model.tables.records.AccountRecord;
import io.cattle.platform.db.jooq.converter.DataConverter;
import io.cattle.platform.db.jooq.converter.DateConverter;

import java.util.Arrays;
import java.util.Collection;
import java.util.Date;
import java.util.List;
import java.util.Map;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableLike;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
public class AccountTable extends TableImpl<AccountRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>cattle.account</code>
     */
    public static final AccountTable ACCOUNT = new AccountTable();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<AccountRecord> getRecordType() {
        return AccountRecord.class;
    }

    /**
     * The column <code>cattle.account.id</code>.
     */
    public final TableField<AccountRecord, Long> ID = createField(DSL.name("id"), SQLDataType.BIGINT.nullable(false).generatedByDefaultAsIdentity(), this, "");

    /**
     * The column <code>cattle.account.name</code>.
     */
    public final TableField<AccountRecord, String> NAME = createField(DSL.name("name"), SQLDataType.VARCHAR(255).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.kind</code>.
     */
    public final TableField<AccountRecord, String> KIND = createField(DSL.name("kind"), SQLDataType.VARCHAR(255).nullable(false), this, "");

    /**
     * The column <code>cattle.account.uuid</code>.
     */
    public final TableField<AccountRecord, String> UUID = createField(DSL.name("uuid"), SQLDataType.VARCHAR(128).nullable(false), this, "");

    /**
     * The column <code>cattle.account.description</code>.
     */
    public final TableField<AccountRecord, String> DESCRIPTION = createField(DSL.name("description"), SQLDataType.VARCHAR(1024).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.state</code>.
     */
    public final TableField<AccountRecord, String> STATE = createField(DSL.name("state"), SQLDataType.VARCHAR(128).nullable(false), this, "");

    /**
     * The column <code>cattle.account.created</code>.
     */
    public final TableField<AccountRecord, Date> CREATED = createField(DSL.name("created"), SQLDataType.LOCALDATETIME(0).defaultValue(DSL.inline("NULL", SQLDataType.LOCALDATETIME)), this, "", new DateConverter());

    /**
     * The column <code>cattle.account.removed</code>.
     */
    public final TableField<AccountRecord, Date> REMOVED = createField(DSL.name("removed"), SQLDataType.LOCALDATETIME(0).defaultValue(DSL.inline("NULL", SQLDataType.LOCALDATETIME)), this, "", new DateConverter());

    /**
     * The column <code>cattle.account.remove_time</code>.
     */
    public final TableField<AccountRecord, Date> REMOVE_TIME = createField(DSL.name("remove_time"), SQLDataType.LOCALDATETIME(0).defaultValue(DSL.inline("NULL", SQLDataType.LOCALDATETIME)), this, "", new DateConverter());

    /**
     * The column <code>cattle.account.data</code>.
     */
    public final TableField<AccountRecord, Map<String,Object>> DATA = createField(DSL.name("data"), SQLDataType.CLOB(16777215).defaultValue(DSL.inline("NULL", SQLDataType.CLOB)), this, "", new DataConverter());

    /**
     * The column <code>cattle.account.external_id</code>.
     */
    public final TableField<AccountRecord, String> EXTERNAL_ID = createField(DSL.name("external_id"), SQLDataType.VARCHAR(255).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.external_id_type</code>.
     */
    public final TableField<AccountRecord, String> EXTERNAL_ID_TYPE = createField(DSL.name("external_id_type"), SQLDataType.VARCHAR(128).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.health_state</code>.
     */
    public final TableField<AccountRecord, String> HEALTH_STATE = createField(DSL.name("health_state"), SQLDataType.VARCHAR(128).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.project_template_id</code>.
     */
    public final TableField<AccountRecord, Long> PROJECT_TEMPLATE_ID = createField(DSL.name("project_template_id"), SQLDataType.BIGINT.defaultValue(DSL.inline("NULL", SQLDataType.BIGINT)), this, "");

    /**
     * The column <code>cattle.account.default_network_id</code>.
     */
    public final TableField<AccountRecord, Long> DEFAULT_NETWORK_ID = createField(DSL.name("default_network_id"), SQLDataType.BIGINT.defaultValue(DSL.inline("NULL", SQLDataType.BIGINT)), this, "");

    /**
     * The column <code>cattle.account.version</code>.
     */
    public final TableField<AccountRecord, String> VERSION = createField(DSL.name("version"), SQLDataType.VARCHAR(128).defaultValue(DSL.inline("NULL", SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>cattle.account.revision</code>.
     */
    public final TableField<AccountRecord, Long> REVISION = createField(DSL.name("revision"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.inline("0", SQLDataType.BIGINT)), this, "");

    private AccountTable(Name alias, Table<AccountRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private AccountTable(Name alias, Table<AccountRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>cattle.account</code> table reference
     */
    public AccountTable(String alias) {
        this(DSL.name(alias), ACCOUNT);
    }

    /**
     * Create an aliased <code>cattle.account</code> table reference
     */
    public AccountTable(Name alias) {
        this(alias, ACCOUNT);
    }

    /**
     * Create a <code>cattle.account</code> table reference
     */
    public AccountTable() {
        this(DSL.name("account"), null);
    }

    public <O extends Record> AccountTable(Table<O> path, ForeignKey<O, AccountRecord> childPath, InverseForeignKey<O, AccountRecord> parentPath) {
        super(path, childPath, parentPath, ACCOUNT);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class AccountPath extends AccountTable implements Path<AccountRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> AccountPath(Table<O> path, ForeignKey<O, AccountRecord> childPath, InverseForeignKey<O, AccountRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private AccountPath(Name alias, Table<AccountRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public AccountPath as(String alias) {
            return new AccountPath(DSL.name(alias), this);
        }

        @Override
        public AccountPath as(Name alias) {
            return new AccountPath(alias, this);
        }

        @Override
        public AccountPath as(Table<?> alias) {
            return new AccountPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : CattleTable.CATTLE;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.ACCOUNT_IDX_ACCOUNT_NAME, Indexes.ACCOUNT_IDX_ACCOUNT_REMOVE_TIME, Indexes.ACCOUNT_IDX_ACCOUNT_REMOVED, Indexes.ACCOUNT_IDX_ACCOUNT_STATE, Indexes.ACCOUNT_IDX_EXTERNAL_IDS);
    }

    @Override
    public Identity<AccountRecord, Long> getIdentity() {
        return Internal.createIdentity(this, ID);
    }

    @Override
    public UniqueKey<AccountRecord> getPrimaryKey() {
        return Keys.KEY_ACCOUNT_PRIMARY;
    }

    @Override
    public List<UniqueKey<AccountRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.KEY_ACCOUNT_IDX_ACCOUNT_UUID);
    }

    @Override
    public List<ForeignKey<AccountRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_ACCOUNT__PROJECT_TEMPLATE_ID);
    }

    private transient ProjectTemplatePath _projectTemplate;

    /**
     * Get the implicit join path to the <code>cattle.project_template</code>
     * table.
     */
    public ProjectTemplatePath projectTemplate() {
        if (_projectTemplate == null)
            _projectTemplate = new ProjectTemplatePath(this, Keys.FK_ACCOUNT__PROJECT_TEMPLATE_ID, null);

        return _projectTemplate;
    }

    private transient AuthTokenPath _authTokenIbfk_1;

    /**
     * Get the implicit to-many join path to the <code>cattle.auth_token</code>
     * table, via the <code>auth_token_ibfk_1</code> key
     */
    public AuthTokenPath authTokenIbfk_1() {
        if (_authTokenIbfk_1 == null)
            _authTokenIbfk_1 = new AuthTokenPath(this, null, Keys.AUTH_TOKEN_IBFK_1.getInverseKey());

        return _authTokenIbfk_1;
    }

    private transient AccountLinkPath _fkAccountLink_AccountId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.account_link</code> table, via the
     * <code>fk_account_link__account_id</code> key
     */
    public AccountLinkPath fkAccountLink_AccountId() {
        if (_fkAccountLink_AccountId == null)
            _fkAccountLink_AccountId = new AccountLinkPath(this, null, Keys.FK_ACCOUNT_LINK__ACCOUNT_ID.getInverseKey());

        return _fkAccountLink_AccountId;
    }

    private transient AccountLinkPath _fkAccountLink_LinkedAccountId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.account_link</code> table, via the
     * <code>fk_account_link__linked_account_id</code> key
     */
    public AccountLinkPath fkAccountLink_LinkedAccountId() {
        if (_fkAccountLink_LinkedAccountId == null)
            _fkAccountLink_LinkedAccountId = new AccountLinkPath(this, null, Keys.FK_ACCOUNT_LINK__LINKED_ACCOUNT_ID.getInverseKey());

        return _fkAccountLink_LinkedAccountId;
    }

    private transient AgentPath _agent;

    /**
     * Get the implicit to-many join path to the <code>cattle.agent</code> table
     */
    public AgentPath agent() {
        if (_agent == null)
            _agent = new AgentPath(this, null, Keys.FK_AGENT__ACCOUNT_ID.getInverseKey());

        return _agent;
    }

    private transient AuditLogPath _fkAuditLog_AccountId;

    /**
     * Get the implicit to-many join path to the <code>cattle.audit_log</code>
     * table, via the <code>fk_audit_log__account_id</code> key
     */
    public AuditLogPath fkAuditLog_AccountId() {
        if (_fkAuditLog_AccountId == null)
            _fkAuditLog_AccountId = new AuditLogPath(this, null, Keys.FK_AUDIT_LOG__ACCOUNT_ID.getInverseKey());

        return _fkAuditLog_AccountId;
    }

    private transient AuditLogPath _fkAuditLog_AuthenticatedAsAccountId;

    /**
     * Get the implicit to-many join path to the <code>cattle.audit_log</code>
     * table, via the <code>fk_audit_log__authenticated_as_account_id</code> key
     */
    public AuditLogPath fkAuditLog_AuthenticatedAsAccountId() {
        if (_fkAuditLog_AuthenticatedAsAccountId == null)
            _fkAuditLog_AuthenticatedAsAccountId = new AuditLogPath(this, null, Keys.FK_AUDIT_LOG__AUTHENTICATED_AS_ACCOUNT_ID.getInverseKey());

        return _fkAuditLog_AuthenticatedAsAccountId;
    }

    private transient AuthTokenPath _fkAuthToken_AccountId;

    /**
     * Get the implicit to-many join path to the <code>cattle.auth_token</code>
     * table, via the <code>fk_auth_token__account_id</code> key
     */
    public AuthTokenPath fkAuthToken_AccountId() {
        if (_fkAuthToken_AccountId == null)
            _fkAuthToken_AccountId = new AuthTokenPath(this, null, Keys.FK_AUTH_TOKEN__ACCOUNT_ID.getInverseKey());

        return _fkAuthToken_AccountId;
    }

    private transient BackupPath _backup;

    /**
     * Get the implicit to-many join path to the <code>cattle.backup</code>
     * table
     */
    public BackupPath backup() {
        if (_backup == null)
            _backup = new BackupPath(this, null, Keys.FK_BACKUP__ACCOUNT_ID.getInverseKey());

        return _backup;
    }

    private transient BackupTargetPath _backupTarget;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.backup_target</code> table
     */
    public BackupTargetPath backupTarget() {
        if (_backupTarget == null)
            _backupTarget = new BackupTargetPath(this, null, Keys.FK_BACKUP_TARGET__ACCOUNT_ID.getInverseKey());

        return _backupTarget;
    }

    private transient CertificatePath _certificate;

    /**
     * Get the implicit to-many join path to the <code>cattle.certificate</code>
     * table
     */
    public CertificatePath certificate() {
        if (_certificate == null)
            _certificate = new CertificatePath(this, null, Keys.FK_CERT_DATA__ACCOUNT_ID.getInverseKey());

        return _certificate;
    }

    private transient ConfigItemStatusPath _configItemStatus;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.config_item_status</code> table
     */
    public ConfigItemStatusPath configItemStatus() {
        if (_configItemStatus == null)
            _configItemStatus = new ConfigItemStatusPath(this, null, Keys.FK_CONFIG_ITEM__ACCOUNT_ID.getInverseKey());

        return _configItemStatus;
    }

    private transient ContainerEventPath _containerEvent;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.container_event</code> table
     */
    public ContainerEventPath containerEvent() {
        if (_containerEvent == null)
            _containerEvent = new ContainerEventPath(this, null, Keys.FK_CONTAINER_EVENT__ACCOUNT_ID.getInverseKey());

        return _containerEvent;
    }

    private transient CredentialPath _credential;

    /**
     * Get the implicit to-many join path to the <code>cattle.credential</code>
     * table
     */
    public CredentialPath credential() {
        if (_credential == null)
            _credential = new CredentialPath(this, null, Keys.FK_CREDENTIAL__ACCOUNT_ID.getInverseKey());

        return _credential;
    }

    private transient DeploymentUnitPath _deploymentUnit;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.deployment_unit</code> table
     */
    public DeploymentUnitPath deploymentUnit() {
        if (_deploymentUnit == null)
            _deploymentUnit = new DeploymentUnitPath(this, null, Keys.FK_DEPLOYMENT_UNIT__ACCOUNT_ID.getInverseKey());

        return _deploymentUnit;
    }

    private transient DynamicSchemaPath _dynamicSchema;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.dynamic_schema</code> table
     */
    public DynamicSchemaPath dynamicSchema() {
        if (_dynamicSchema == null)
            _dynamicSchema = new DynamicSchemaPath(this, null, Keys.FK_DYNAMIC_SCHEMA__ACCOUNT_ID.getInverseKey());

        return _dynamicSchema;
    }

    private transient EnvironmentPath _environment;

    /**
     * Get the implicit to-many join path to the <code>cattle.environment</code>
     * table
     */
    public EnvironmentPath environment() {
        if (_environment == null)
            _environment = new EnvironmentPath(this, null, Keys.FK_ENVIRONMENT__ACCOUNT_ID.getInverseKey());

        return _environment;
    }

    private transient ExternalEventPath _fkExternalEvent_AccountId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.external_event</code> table, via the
     * <code>fk_external_event__account_id</code> key
     */
    public ExternalEventPath fkExternalEvent_AccountId() {
        if (_fkExternalEvent_AccountId == null)
            _fkExternalEvent_AccountId = new ExternalEventPath(this, null, Keys.FK_EXTERNAL_EVENT__ACCOUNT_ID.getInverseKey());

        return _fkExternalEvent_AccountId;
    }

    private transient ExternalEventPath _fkExternalEvent_ReportedAccountId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.external_event</code> table, via the
     * <code>fk_external_event__reported_account_id</code> key
     */
    public ExternalEventPath fkExternalEvent_ReportedAccountId() {
        if (_fkExternalEvent_ReportedAccountId == null)
            _fkExternalEvent_ReportedAccountId = new ExternalEventPath(this, null, Keys.FK_EXTERNAL_EVENT__REPORTED_ACCOUNT_ID.getInverseKey());

        return _fkExternalEvent_ReportedAccountId;
    }

    private transient GenericObjectPath _genericObject;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.generic_object</code> table
     */
    public GenericObjectPath genericObject() {
        if (_genericObject == null)
            _genericObject = new GenericObjectPath(this, null, Keys.FK_GENERIC_OBJECT__ACCOUNT_ID.getInverseKey());

        return _genericObject;
    }

    private transient HealthcheckInstancePath _healthcheckInstance;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.healthcheck_instance</code> table
     */
    public HealthcheckInstancePath healthcheckInstance() {
        if (_healthcheckInstance == null)
            _healthcheckInstance = new HealthcheckInstancePath(this, null, Keys.FK_HEALTHCHECK_INSTANCE__ACCOUNT_ID.getInverseKey());

        return _healthcheckInstance;
    }

    private transient HealthcheckInstanceHostMapPath _healthcheckInstanceHostMap;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.healthcheck_instance_host_map</code> table
     */
    public HealthcheckInstanceHostMapPath healthcheckInstanceHostMap() {
        if (_healthcheckInstanceHostMap == null)
            _healthcheckInstanceHostMap = new HealthcheckInstanceHostMapPath(this, null, Keys.FK_HEALTHCHECK_INSTANCE_HOST_MAP__ACCOUNT_ID.getInverseKey());

        return _healthcheckInstanceHostMap;
    }

    private transient HostPath _host;

    /**
     * Get the implicit to-many join path to the <code>cattle.host</code> table
     */
    public HostPath host() {
        if (_host == null)
            _host = new HostPath(this, null, Keys.FK_HOST__ACCOUNT_ID.getInverseKey());

        return _host;
    }

    private transient HostLabelMapPath _hostLabelMap;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.host_label_map</code> table
     */
    public HostLabelMapPath hostLabelMap() {
        if (_hostLabelMap == null)
            _hostLabelMap = new HostLabelMapPath(this, null, Keys.FK_HOST_LABEL_MAP__ACCOUNT_ID.getInverseKey());

        return _hostLabelMap;
    }

    private transient HostTemplatePath _hostTemplate;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.host_template</code> table
     */
    public HostTemplatePath hostTemplate() {
        if (_hostTemplate == null)
            _hostTemplate = new HostTemplatePath(this, null, Keys.FK_HOST_TEMPLATE__ACCOUNT_ID.getInverseKey());

        return _hostTemplate;
    }

    private transient ImagePath _image;

    /**
     * Get the implicit to-many join path to the <code>cattle.image</code> table
     */
    public ImagePath image() {
        if (_image == null)
            _image = new ImagePath(this, null, Keys.FK_IMAGE__ACCOUNT_ID.getInverseKey());

        return _image;
    }

    private transient InstancePath _instance;

    /**
     * Get the implicit to-many join path to the <code>cattle.instance</code>
     * table
     */
    public InstancePath instance() {
        if (_instance == null)
            _instance = new InstancePath(this, null, Keys.FK_INSTANCE__ACCOUNT_ID.getInverseKey());

        return _instance;
    }

    private transient InstanceLabelMapPath _instanceLabelMap;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.instance_label_map</code> table
     */
    public InstanceLabelMapPath instanceLabelMap() {
        if (_instanceLabelMap == null)
            _instanceLabelMap = new InstanceLabelMapPath(this, null, Keys.FK_INSTANCE_LABEL_MAP__ACCOUNT_ID.getInverseKey());

        return _instanceLabelMap;
    }

    private transient IpAddressPath _ipAddress;

    /**
     * Get the implicit to-many join path to the <code>cattle.ip_address</code>
     * table
     */
    public IpAddressPath ipAddress() {
        if (_ipAddress == null)
            _ipAddress = new IpAddressPath(this, null, Keys.FK_IP_ADDRESS__ACCOUNT_ID.getInverseKey());

        return _ipAddress;
    }

    private transient LabelPath _label;

    /**
     * Get the implicit to-many join path to the <code>cattle.label</code> table
     */
    public LabelPath label() {
        if (_label == null)
            _label = new LabelPath(this, null, Keys.FK_LABEL__ACCOUNT_ID.getInverseKey());

        return _label;
    }

    private transient InstanceLinkPath _instanceLink;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.instance_link</code> table
     */
    public InstanceLinkPath instanceLink() {
        if (_instanceLink == null)
            _instanceLink = new InstanceLinkPath(this, null, Keys.FK_LINK__ACCOUNT_ID.getInverseKey());

        return _instanceLink;
    }

    private transient MountPath _mount;

    /**
     * Get the implicit to-many join path to the <code>cattle.mount</code> table
     */
    public MountPath mount() {
        if (_mount == null)
            _mount = new MountPath(this, null, Keys.FK_MOUNT__ACCOUNT_ID.getInverseKey());

        return _mount;
    }

    private transient NetworkPath _network;

    /**
     * Get the implicit to-many join path to the <code>cattle.network</code>
     * table
     */
    public NetworkPath network() {
        if (_network == null)
            _network = new NetworkPath(this, null, Keys.FK_NETWORK__ACCOUNT_ID.getInverseKey());

        return _network;
    }

    private transient NetworkDriverPath _networkDriver;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.network_driver</code> table
     */
    public NetworkDriverPath networkDriver() {
        if (_networkDriver == null)
            _networkDriver = new NetworkDriverPath(this, null, Keys.FK_NETWORK_DRIVER__ACCOUNT_ID.getInverseKey());

        return _networkDriver;
    }

    private transient NicPath _nic;

    /**
     * Get the implicit to-many join path to the <code>cattle.nic</code> table
     */
    public NicPath nic() {
        if (_nic == null)
            _nic = new NicPath(this, null, Keys.FK_NIC__ACCOUNT_ID.getInverseKey());

        return _nic;
    }

    private transient PhysicalHostPath _physicalHost;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.physical_host</code> table
     */
    public PhysicalHostPath physicalHost() {
        if (_physicalHost == null)
            _physicalHost = new PhysicalHostPath(this, null, Keys.FK_PHYSICAL_HOST__ACCOUNT_ID.getInverseKey());

        return _physicalHost;
    }

    private transient PortPath _port;

    /**
     * Get the implicit to-many join path to the <code>cattle.port</code> table
     */
    public PortPath port() {
        if (_port == null)
            _port = new PortPath(this, null, Keys.FK_PORT__ACCOUNT_ID.getInverseKey());

        return _port;
    }

    private transient ProcessInstancePath _processInstance;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.process_instance</code> table
     */
    public ProcessInstancePath processInstance() {
        if (_processInstance == null)
            _processInstance = new ProcessInstancePath(this, null, Keys.FK_PROCESS_INSTANCE__ACCOUNT_ID.getInverseKey());

        return _processInstance;
    }

    private transient ProjectMemberPath _fkProjectMember_AccountId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.project_member</code> table, via the
     * <code>fk_project_member__account_id</code> key
     */
    public ProjectMemberPath fkProjectMember_AccountId() {
        if (_fkProjectMember_AccountId == null)
            _fkProjectMember_AccountId = new ProjectMemberPath(this, null, Keys.FK_PROJECT_MEMBER__ACCOUNT_ID.getInverseKey());

        return _fkProjectMember_AccountId;
    }

    private transient ProjectMemberPath _fkProjectMember_ProjectId;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.project_member</code> table, via the
     * <code>fk_project_member__project_id</code> key
     */
    public ProjectMemberPath fkProjectMember_ProjectId() {
        if (_fkProjectMember_ProjectId == null)
            _fkProjectMember_ProjectId = new ProjectMemberPath(this, null, Keys.FK_PROJECT_MEMBER__PROJECT_ID.getInverseKey());

        return _fkProjectMember_ProjectId;
    }

    private transient RegionPath _region;

    /**
     * Get the implicit to-many join path to the <code>cattle.region</code>
     * table
     */
    public RegionPath region() {
        if (_region == null)
            _region = new RegionPath(this, null, Keys.FK_REGION__ACCOUNT_ID.getInverseKey());

        return _region;
    }

    private transient ResourcePoolPath _resourcePool;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.resource_pool</code> table
     */
    public ResourcePoolPath resourcePool() {
        if (_resourcePool == null)
            _resourcePool = new ResourcePoolPath(this, null, Keys.FK_RESOURCE_POOL__ACCOUNT_ID.getInverseKey());

        return _resourcePool;
    }

    private transient ScheduledUpgradePath _scheduledUpgrade;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.scheduled_upgrade</code> table
     */
    public ScheduledUpgradePath scheduledUpgrade() {
        if (_scheduledUpgrade == null)
            _scheduledUpgrade = new ScheduledUpgradePath(this, null, Keys.FK_SCHEDULED_UPGRADE__ACCOUNT_ID.getInverseKey());

        return _scheduledUpgrade;
    }

    private transient SecretPath _secret;

    /**
     * Get the implicit to-many join path to the <code>cattle.secret</code>
     * table
     */
    public SecretPath secret() {
        if (_secret == null)
            _secret = new SecretPath(this, null, Keys.FK_SECRET__ACCOUNT_ID.getInverseKey());

        return _secret;
    }

    private transient ServicePath _service;

    /**
     * Get the implicit to-many join path to the <code>cattle.service</code>
     * table
     */
    public ServicePath service() {
        if (_service == null)
            _service = new ServicePath(this, null, Keys.FK_SERVICE__ACCOUNT_ID.getInverseKey());

        return _service;
    }

    private transient ServiceConsumeMapPath _serviceConsumeMap;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.service_consume_map</code> table
     */
    public ServiceConsumeMapPath serviceConsumeMap() {
        if (_serviceConsumeMap == null)
            _serviceConsumeMap = new ServiceConsumeMapPath(this, null, Keys.FK_SERVICE_CONSUME_MAP__ACCOUNT_ID.getInverseKey());

        return _serviceConsumeMap;
    }

    private transient ServiceEventPath _serviceEvent;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.service_event</code> table
     */
    public ServiceEventPath serviceEvent() {
        if (_serviceEvent == null)
            _serviceEvent = new ServiceEventPath(this, null, Keys.FK_SERVICE_EVENT__ACCOUNT_ID.getInverseKey());

        return _serviceEvent;
    }

    private transient ServiceExposeMapPath _serviceExposeMap;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.service_expose_map</code> table
     */
    public ServiceExposeMapPath serviceExposeMap() {
        if (_serviceExposeMap == null)
            _serviceExposeMap = new ServiceExposeMapPath(this, null, Keys.FK_SERVICE_EXPOSE_MAP__ACCOUNT_ID.getInverseKey());

        return _serviceExposeMap;
    }

    private transient ServiceLogPath _serviceLog;

    /**
     * Get the implicit to-many join path to the <code>cattle.service_log</code>
     * table
     */
    public ServiceLogPath serviceLog() {
        if (_serviceLog == null)
            _serviceLog = new ServiceLogPath(this, null, Keys.FK_SERVICE_LOG__ACCOUNT_ID.getInverseKey());

        return _serviceLog;
    }

    private transient ServiceIndexPath _serviceIndex;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.service_index</code> table
     */
    public ServiceIndexPath serviceIndex() {
        if (_serviceIndex == null)
            _serviceIndex = new ServiceIndexPath(this, null, Keys.FK_SERVICE_SUFFIX__ACCOUNT_ID.getInverseKey());

        return _serviceIndex;
    }

    private transient SnapshotPath _snapshot;

    /**
     * Get the implicit to-many join path to the <code>cattle.snapshot</code>
     * table
     */
    public SnapshotPath snapshot() {
        if (_snapshot == null)
            _snapshot = new SnapshotPath(this, null, Keys.FK_SNAPSHOT__ACCOUNT_ID.getInverseKey());

        return _snapshot;
    }

    private transient StorageDriverPath _storageDriver;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.storage_driver</code> table
     */
    public StorageDriverPath storageDriver() {
        if (_storageDriver == null)
            _storageDriver = new StorageDriverPath(this, null, Keys.FK_STORAGE_DRIVER__ACCOUNT_ID.getInverseKey());

        return _storageDriver;
    }

    private transient StoragePoolPath _storagePool;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.storage_pool</code> table
     */
    public StoragePoolPath storagePool() {
        if (_storagePool == null)
            _storagePool = new StoragePoolPath(this, null, Keys.FK_STORAGE_POOL__ACCOUNT_ID.getInverseKey());

        return _storagePool;
    }

    private transient SubnetPath _subnet;

    /**
     * Get the implicit to-many join path to the <code>cattle.subnet</code>
     * table
     */
    public SubnetPath subnet() {
        if (_subnet == null)
            _subnet = new SubnetPath(this, null, Keys.FK_SUBNET__ACCOUNT_ID.getInverseKey());

        return _subnet;
    }

    private transient UserPreferencePath _userPreference;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.user_preference</code> table
     */
    public UserPreferencePath userPreference() {
        if (_userPreference == null)
            _userPreference = new UserPreferencePath(this, null, Keys.FK_USER_PREFERENCE__ACCOUNT_ID.getInverseKey());

        return _userPreference;
    }

    private transient VolumePath _volume;

    /**
     * Get the implicit to-many join path to the <code>cattle.volume</code>
     * table
     */
    public VolumePath volume() {
        if (_volume == null)
            _volume = new VolumePath(this, null, Keys.FK_VOLUME__ACCOUNT_ID.getInverseKey());

        return _volume;
    }

    private transient VolumeTemplatePath _volumeTemplate;

    /**
     * Get the implicit to-many join path to the
     * <code>cattle.volume_template</code> table
     */
    public VolumeTemplatePath volumeTemplate() {
        if (_volumeTemplate == null)
            _volumeTemplate = new VolumeTemplatePath(this, null, Keys.FK_VOLUME_TEMPLATE__ACCOUNT_ID.getInverseKey());

        return _volumeTemplate;
    }

    private transient ZonePath _zone;

    /**
     * Get the implicit to-many join path to the <code>cattle.zone</code> table
     */
    public ZonePath zone() {
        if (_zone == null)
            _zone = new ZonePath(this, null, Keys.FK_ZONE__ACCOUNT_ID.getInverseKey());

        return _zone;
    }

    @Override
    public AccountTable as(String alias) {
        return new AccountTable(DSL.name(alias), this);
    }

    @Override
    public AccountTable as(Name alias) {
        return new AccountTable(alias, this);
    }

    @Override
    public AccountTable as(Table<?> alias) {
        return new AccountTable(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public AccountTable rename(String name) {
        return new AccountTable(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public AccountTable rename(Name name) {
        return new AccountTable(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public AccountTable rename(Table<?> name) {
        return new AccountTable(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable where(Condition condition) {
        return new AccountTable(getQualifiedName(), aliased() ? this : null, null, Internal.condition(this, condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public AccountTable where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public AccountTable where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public AccountTable where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public AccountTable where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable whereExists(TableLike<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public AccountTable whereNotExists(TableLike<?> select) {
        return where(DSL.notExists(select));
    }
}
