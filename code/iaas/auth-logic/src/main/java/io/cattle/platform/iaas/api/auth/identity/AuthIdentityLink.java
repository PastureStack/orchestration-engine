package io.cattle.platform.iaas.api.auth.identity;

/**
 * API schema marker for a stable account-to-login-identity link.
 *
 * <p>The public fields and methods are defined by
 * {@code schema/base/authIdentityLink.json}. Keeping a concrete marker type
 * makes the schema part of the generated and frozen v1 schema sets.</p>
 */
public interface AuthIdentityLink {
}
